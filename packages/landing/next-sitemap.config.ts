import type { IConfig } from 'next-sitemap';

const config: IConfig = {
  siteUrl: 'https://pw56.github.io/web-puri-project',
  generateRobotsTxt: false,
  outDir: './out',
  changefreq: 'daily',
  priority: 1,
}

export default config;